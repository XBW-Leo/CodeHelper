---
description: 按已有计划实施，边实现边验证
argument-hint: "[阶段或任务描述]"
---
请按当前对话中已确认的计划实施。附加说明：${@:-无}

要求：
1. 如果对话中还没有计划，先简要列出行动计划，再开始实现。
2. 小步实现：一次只改一个相关文件组，改完立即用编译/测试/校验验证。
3. 遵循 AGENTS.md 中的项目约定（校验命令、Git 规则、文档同步）。
4. 不引入无关改动；不删除看起来有意的功能；不为了通过测试而放宽断言。
5. 完成后报告：改了什么、验证结果、遗留问题。
